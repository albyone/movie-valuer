from pathlib import Path

OUTPUT_COLUMNS = [
    "Estimated Value (AUD)",
    "Confidence",
    "Source",
    "Notes",
    "Last Checked",
]

OUTPUT_DIR = Path("output")
CACHE_DIR = Path("cache")
LOG_DIR = Path("logs")